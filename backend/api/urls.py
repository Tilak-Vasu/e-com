# backend/api/urls.py

from django.urls import path
# We will add our protected views here later

urlpatterns = [
    # For now, this list is empty.
    # We will add routes like path('orders/', ...) here in the future.
]